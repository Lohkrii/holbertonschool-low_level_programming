#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase letters
 *
 * Return: Must return 0 for success
*/

int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
	putchar(x);
	}
	return (0);
}
